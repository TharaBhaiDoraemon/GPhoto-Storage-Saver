# Dependencies
node_modules/
source/node_modules/

# User downloaded media and temporary files
downloads/*
!downloads/.gitkeep
!downloads/README.md

# Scan metadata, manifests, and backups
manifest.json
manifest*.json
*.bak*
progress.json
progress.backup.json
quota-items-scan.json

# Browser automation profile
.browser-profile/

# Operating system & editor files
.DS_Store
Thumbs.db
*.log

# Windows installer build artifacts (rebuild with installer/build.sh)
installer/build/
installer/dist/

# Debian package build artifacts (rebuild with deb/build.sh)
deb/build/
deb/dist/

# AppImage build artifacts (rebuild with appimage/build.sh)
appimage/build/
appimage/dist/

# macOS .app build artifacts (rebuild with mac/build.sh)
mac/build/
mac/dist/
